<h1>Editing Book</h1>
  <%= form_for(@book) do |f| %>

    <% if @book.errors.any? %>
      <% if @book.errors.any? %>
      <%= @book.errors.count %> errors prohibited this book from being saved:<br>
  <ul>
    <% @book.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
  <% end %>
    <% end%>

      <h4>Title</h4>
        <%= f.text_field :title %>
      <h4>Body</h4>
        <%= f.text_area :body %>
        <div class="Update_box"><%= f.submit "Update Book" %></div>
  <% end %>

    <%= link_to "Show", book_path(@book) %>
    |
    <%= link_to "Back", books_path %>